name: Process Image Manually

on:
  workflow_dispatch:
    inputs:
      image_name:
        description: 'The name of the image to process'
        required: true
        default: 'image.jpg'

jobs:
  process-image:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: '3.x'

      - name: Install dependencies
        run: |
          sudo apt-get update
          python -m pip install --upgrade pip
          pip install pillow torch transformers numpy  # Install Pillow, PyTorch, Transformers, and NumPy

      - name: Run image processing script
        run: |
          # Combine category and image name into the full path
          IMAGE_PATH="images/${{ github.event.inputs.image_name }}"
          python process_image.py $IMAGE_PATH
